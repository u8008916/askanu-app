# DECISION_LOG.md

Record only decisions that change V3 contracts, architecture, source policy, scope, security/privacy rules, or schedule.

| Date | Decision | Why | Affected repos/docs | Owner | Approved by |
|---|---|---|---|---|---|
| 2026-09-12 | Resource pages become compact **guided-intent launchers**: 3–5 recommended-question cards on top, compact official links at the bottom. A card places its question in the single chat composer as an editable draft, focuses it and navigates to `/`; it never auto-sends and does not clear the current conversation. Supersedes the V3 wording "resource pages are information hubs" (`V3_LOCKED_DECISIONS.md` §Product/UI, `AGENTS.md` locked UX). Still one chatbot, still `Clear Chat`, still no chat input on domain pages. Desktop no-scroll acceptance viewport: 1280×720. | AskANU V5 Execution Plan §3 "V5 product/UX contract: guided domain launchers" and §1 "What changed from V4". | askanu-app: `AGENTS.md`, `docs/V3_LOCKED_DECISIONS.md`, `my_day_by_day_tasks.md`. No API/schema/source change. | Ben | Qasim — PR #27 review, 12 Sep 2026: 1280×720 frozen as the minimum supported desktop viewport; card click keeps the current session (only explicit Clear Chat resets); plain question text into the existing composer, no new `/api/v1/ask` field |
| 2026-09-15 | **Contract sync, not a new decision:** this repo's `API_CONTRACT.md` `GET /api/v1/jobs/current` item shape is aligned to what the RAG service ships and documents (`employment_types[]`, `job_id`, `classification`, `salary`, `closing_text`, `closing_date`, `closing_at`, `status: "current"`; limit range 1–20), replacing the pre-schema `employment_type`/`closing_at`-only sketch. The App service now forwards `GET /api/v1/jobs/current` and `GET /api/v1/events/upcoming` to RAG unchanged (only a numeric `limit` is relayed), and the Current Jobs / Upcoming Events panels render the real lists with loading / list / empty / unavailable states and no placeholder rows. `/api/v1/events/upcoming` is not served by RAG until the Events build day; the panel shows "unavailable" until then. | The RAG shape was reviewed and merged on Day 10 (askanu-rag PR #22) while the App doc still carried the Day 1 sketch; the panels had never been wired (flagged in `docs/evidence/DAY_11_BREADTH_AUDIT.md` §5). V3 "short deterministic Upcoming Events and Current Jobs summaries" needs the real path. | askanu-app: `docs/API_CONTRACT.md`, `server/src/server.js`, `frontend/src/types/api.ts`, `frontend/src/resources/*`. askanu-rag: none (its contract is the source). | Ben | Qasim — pending review of this PR |
| 2026-09-19 | **Contract sync, not a new decision:** this repo's `API_CONTRACT.md` `GET /api/v1/events/upcoming` item shape is aligned to what the RAG service ships on its Events build day (adds nullable `status` = stored cancellation wording, else stored source status; fixes `source_id` to `events_anu_official`; `record_id = events:event:<entity_id>`; limit range 1–20; official-only surface, Rubric never on the Upcoming panel). The Events guided page (`/events`) ships as the sixth `DomainLauncher` config, the nav entry becomes a real link, the Upcoming Events panel gains `View all` and shows venue/organiser/status as stored, and the Home `Try asking` events suggestion uses wording the backend resolves. No new `/api/v1/ask` field; the App does no source or status filtering (Qasim, 19 Sep). | The RAG shape landed in askanu-rag `carmen/day15-events-rag-api` (19 Sep) while the App doc still carried the Day 1 sketch (flagged in `docs/evidence/DAY_11B_LIST_PANELS_APP_PATH.md`). V3 "Events: official ANU Events/calendar" + "Rubric disabled for production unless approved" need the real path. | askanu-app: `docs/API_CONTRACT.md`, `frontend/src/types/api.ts`, `frontend/src/resources/*`, `frontend/src/domains/domainConfig.ts`, `frontend/src/layout/DomainNav.tsx`. askanu-rag: none (its contract is the source). | Ben | Qasim — Day 15 PM classification given 19 Sep 2026 (recorded in `docs/evidence/DAY_15_EVENTS_SIX_DOMAIN.md` §1); PR review pending |

If a decision changes a shared contract, update every affected repo in the same work cycle.
